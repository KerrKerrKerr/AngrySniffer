buttons [text fields near them to get Strings from] (description of meachnics) "text field near it to display something":
    list interfaces
    set interface [interface]
    set monitor [monitor]
    add moinitor [monitor name]
    down [interface name]
    kill network services
    lift network services
    start collecting network list
    stop collecting network list
    choose target AP ESSID (child winodow with table should appear where one AP should be chosen) "<ESSID>"
    shoose target station (child winodow with table of station for selected AP should appear where one station should be chosen) //for future
    deauth "<ESSID> of AP and target <MAC>" //for future
    start capturing "selected: <ESSID>" (opens new terminal with scanning process, so no stop button)
    
    

buttons are stacked in the ledt corner of the window occupying 100% of vertical and 40& of horizontal space

console occupies rest